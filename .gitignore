node_modules/
dist/
.DS_Store
*.local

# Secrets. The .example files are templates with empty values and stay tracked.
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# Packaged builds for the Web Store; rebuild from dist/ with `bun run build`.
*.zip

# Local agent tooling, not part of the extension.
.agents/
skill-observations/
cross-cutting-principles.md
skills-lock.json
